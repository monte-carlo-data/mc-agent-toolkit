[project]
name = "mc-agent-toolkit-evals"
version = "0.1.0"
description = "Trigger accuracy evals for mc-agent-toolkit skills"
requires-python = ">=3.11"
dependencies = ["anthropic>=0.40"]

[tool.uv]
dev-dependencies = []
