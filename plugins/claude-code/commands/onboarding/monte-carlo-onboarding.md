---
description: Connect a warehouse to Monte Carlo — choose the deployment, reference credentials, create the warehouse and connection (REST API v2 tools)
---

Activate the Monte Carlo Onboarding skill. Follow its steps in order: frame the run (what to connect, output mode: act now / Terraform / SDK-CLI script), `list_deployments` and resolve the deployment (reuse, collection agent, or data store — never one with nothing behind it), credentials by reference (no secret through the chat), warehouse, connection, "validate in the UI", and end with the summary of created ids.

**User provided input**: $ARGUMENTS
