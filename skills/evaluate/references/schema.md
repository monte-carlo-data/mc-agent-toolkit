# Case YAML Schema and Validation

Placeholder. Filled in by slice 1 (schema validation is in-scope for slice 1 per CLAUDE.md).

The authoritative case schema lives in `SPEC.md` §7 in this directory. This reference file will describe the validation procedure the skill runs when reading or writing case YAMLs.
